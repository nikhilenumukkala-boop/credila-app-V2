# Credila App V2

Interactive HTML prototype of the Credila customer app (v2) — Phase 1 requirements.

Single-file app: open `index.html` in any browser, no build step needed.

- Designed in Cowork: "Credila phase 1 requirements"
- Deployed on Vercel as a static site (`vercel.json` config included)
